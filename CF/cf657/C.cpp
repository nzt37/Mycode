#include <bits/stdc++.h>

using namespace std;
int n,m;
void solve()
{
	std::vector<int> a(n);
	cin >> n >> m;
	for(int i = 1; i <= n; i++) {
		cin >> a[i];
	}
	for(int i = 1; i <= n; i++) {
		cin >> a[i];
	}
}
int main()
{
	int T;
	cin >> T;
	while(T--) {
		solve();
	}
	return 0;
}